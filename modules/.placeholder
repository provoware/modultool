# Beispiel für ein einfaches Modul (Programmteil)
# Speichere deinen Code in eigenen Dateien im Ordner modules/.
# Die Datei beispiel_modul.js zeigt, wie es geht.

# --- Start des Minimalbeispiels (JavaScript) ---
export function begruessung(name = 'Welt') {
  console.log(`Hallo ${name}!`);
}
# --- Ende des Minimalbeispiels ---
